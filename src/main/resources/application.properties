# Server configuration
server.port=8080

# Actuator endpoints
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Application info
info.app.name=Spring Boot Azure
info.app.description=Spring Boot application deployed to Azure
info.app.version=1.0.0